{
  "name": "skylink",
  "version": "1.0.0",
  "private": true,
  "description": "SkyLink full-stack airline booking system with React, Express, and Microsoft SQL Server.",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w server\" \"npm run dev -w client\"",
    "build": "npm run build -w client",
    "start": "npm run start -w server"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
